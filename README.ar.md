<!--
  ___ _ _     _    _    _           
 | __(_) |___| |  (_)__| |_ ___ _ _ 
 | _|| | / -_) |__| (_-<  _/ -_) '_|
 |_| |_|_\___|____|_/__/\__\___|_|  
 FileLister by TRONG.PRO
-->

# 📂 FileLister: سكريبت قائمة دليل PHP حديث v1.5.36

FileLister هو سكريبت قوائم دليل PHP قوي وخفيف الوزن وحديث يحول ملفات الخادم إلى مستكشف ملفات ويب جميل وسهل الاستخدام على الهواتف المحمولة. إنه البديل المثالي لـ `h5ai` أو `Apache Index`، مع خيار نشر ملف واحد ومعاينات ملفات مدمجة.

![PHP Version](https://img.shields.io/badge/PHP-%3E%3D5.2-blue)
![License](https://img.shields.io/badge/license-MIT-green)
![Version](https://img.shields.io/badge/version-1.5.36-orange)
[![GitHub](https://img.shields.io/badge/GitHub-Repo-181717?logo=github)](https://github.com/daovantrong/filelister)

[🇬🇧 English](README.md) | [🇻🇳 Tiếng Việt](README.vi.md) | [🇨🇳 简体中文](README.zh.md) | [🇪🇸 Español](README.es.md) | [🇫🇷 Français](README.fr.md) | [🇩🇪 Deutsch](README.de.md) | [🇯🇵 日本語](README.ja.md) | [🇰🇷 한국어](README.kr.md) | [🇮🇹 Italiano](README.it.md) | [🇳🇱 Nederlands](README.nl.md) | [🇸🇪 Svenska](README.sv.md) | [🇳🇴 Norsk](README.no.md) | [🇩🇰 Dansk](README.da.md) | [🇫🇮 Suomi](README.fi.md) | [🇮🇱 עברית](README.he.md) | [🇦🇪 العربية](README.ar.md) | [🇷🇺 Русский](README.ru.md)

---

## 📖 جدول المحتويات
- [✨ الميزات](#-الميزات)
- [📦 التثبيت](#-التثبيت)
- [⚙️ التكوين](#-التكوين)
- [🎨 السمات](#-السمات)
- [🧩 خطافات HTML مخصصة](#-خطافات-html-مخصصة)
- [🌍 دعم متعدد اللغات](#-دعم-متعدد-اللغات)
- [👁️ معاينة الملف](#-معاينة-الملف--viewer)
- [🔗 مشاركة & تنزيل](#-مشاركة--تنزيل)
- [⌨️ اختصارات لوحة المفاتيح](#-اختصارات-لوحة-المفاتيح)
- [🛡️ تفاصيل الأمان](#-تفاصيل-الأمان)
- [📋 المتطلبات](#-المتطلبات)

---

## ✨ الميزات

### 🚀 **جاهز للإنتاج & سريع**
- **إصدار مستقل**: نشر ملف واحد (`Standalone.php`) مع تضمين جميع الأصول. قم بتشغيل `php build.php` لإنشاء.
- **دعم Docker**: `Dockerfile` و `docker-compose.yml` جاهزان للاستخدام.
- **خدمة الفهرس**: خدمة اختيارية `index.html` إذا كان موجوداً في دليل.

### 🎨 **واجهة مستخدم حديثة**
- **نظيف & متجاوب**: تخطيط mobile-first، يعمل على جميع الأجهزة.
- **9 سمات**: `light`, `dark`, `auto`, `ocean`, `forest`, `dracula`, `nord`, `high-contrast`, `cute` (anime glassmorphism).
- **عرض الشبكة & القائمة**: التبديل بين عرض شبكة بطاقات وعرض قائمة مفصلة.
- **عرض README**: عرض تلقائي لملفات `README.md` في أسفل قوائم الدليل.
- **محلي**: محدد لغة مع 18+ منطقة مدعومة.

### 🛡️ **أمان محسن**
- **CSP مع Nonces**: nonce مشفر لكل طلب على جميع السكريبتات الداخلية. لا `unsafe-inline`.
- **تحديد المعدل**: throttling طلبات مضاد DDoS مدمج (60 req/60s افتراضياً).
- **وكلاء موثوقين**: معالجة آمنة لـ `X-Forwarded-For` — مطبق فقط إذا كان الطلب من IP وكيل موثوق.
- **حماية مسار الطريق**: جميع إدخال `?dir=` يتم حله عبر `realpath()` وربطه بـ `$lister_root`.
- **إخفاء الملفات الحساسة**: تجاهل تلقائي لـ `.env`, `.git`, `.htaccess`, وملفات PHP.
- **رؤوس الأمان**: `X-Frame-Options`, `X-Content-Type-Options`, `X-XSS-Protection`, `Referrer-Policy`, `Permissions-Policy`, `Strict-Transport-Security` (HTTPS فقط).
- **لا MD5/SHA-1**: مجموعة hash افتراضية محددة إلى `CRC32,XXH128,SHA-256,SHA3-256`. MD5 و SHA-1 مستثنى افتراضياً.

### 🔍 **سلامة الملف (معلومات & Hash)**
- التحقق من 40+ خوارزمية hash لكل ملف، بما في ذلك SHA-3, WHIRLPOOL, XXH128, CRC32.
- حجم ملف أقصى قابل للتكوين للـ hashing.
- النتائج معروضة inline في نموذج المعلومات.

### 📤 **تصدير & مشاركة**
- نسخ/تنزيل قائمة ملفات في **JSON, CSV, TSV, NDJSON** تنسيقات.
- مشاركة الملفات عبر رموز QR ورسائل مباشرة.

---

## 📦 التثبيت & أوضاع النشر

FileLister يدعم 4 أوضاع نشر. اختر الذي يناسب التكوين الخاص بك:

---

### وضع 1: مستقل (ملف PHP واحد) — موصى به للإنتاج

جميع الأصول مترجمة إلى ملف ذاتي الاحتواء. لا حاجة لمجلد `_/`.

```bash
# الخطوة 1: بناء الملف المستقل
php build.php

# الخطوة 2: تحميل Standalone.php إلى الخادم الخاص بك
# الخطوة 3: إعادة تسمية إلى index.php (أو أي اسم تفضله)
```

> **Config**: يحدد تلقائياً `'use_embedded' => true`. لا config إضافي مطلوب.

---

### وضع 2: عادي (ملفات المصدر)

تكوين متعدد الملفات الكلاسيكي. الأسرع للتطوير.

```
your-web-root/
├── index.php        ← نقطة الدخول (require_once 'core.php')
├── core.php         ← منطق النواة & config
└── _/               ← ملفات CSS, JS, أيقونات, ترجمات
```

**الخطوات:**
1. نسخ `index.php`, `core.php`, و `_/` إلى دليل الويب الخاص بك.
2. الوصول عبر المتصفح: `http://yoursite.com/`
3. لا config إضافي مطلوب.

---

### وضع 3: نشر دليل فرعي

تشغيل FileLister في دليل فرعي يحتوي على محتواه الخاص.

```
your-web-root/
├── files/           ← دليل تريد فهرسته
│   ├── index.php    ← نقطة دخول FileLister
│   └── core.php
└── _/               ← أصول مشتركة (auto-مكتشفة عبر فحص الوالد)
```

الدالة `detect_assets_path()` تفحص تلقائياً **حتى 5 دلائل والد** لتحديد مجلد أصول `_/`. لا config `assets_path` يدوي مطلوب في معظم الحالات.

إذا لم يتم اكتشاف الأصول تلقائياً:
```php
'assets_path' => '../_',   // أو مسار كامل مثل '/var/www/html/_'
```

---

### وضع 4: نشر عالمي (فهرسة أي دليل على الخادم)

استخدام **تثبيت FileLister واحد** لتصفح أي مسار على الخادم، منفصل عن موقع السكريبت.

```
/var/www/html/
├── filelister/      ← FileLister يعيش هنا
│   ├── index.php
│   ├── core.php
│   └── _/
└── data/            ← دليل تريد فهرسته فعلياً
```

**تكوين في `core.php`:**
```php
'base_path' => '/var/data',   // ← حدد الدليل الذي تريد قائمته
```

> `base_path` يقبل أي **مسار نظام ملفات مطلق** يمكن لعملية PHP قراءته. السكريبت سيفرض أن تظل جميع تنقل `?dir=` داخل هذا الجذر عبر `realpath()` لمنع مسار الطريق.

**تكوين خادم الويب** (لاستخدام FileLister كفهرس دليل):

**Nginx:**
```nginx
server {
    root /var/data;
    index index.php FileLister.php index.html;

    location / {
        try_files $uri $uri/ /index.php?$query_string;
    }
    location ~ \.php$ {
        fastcgi_pass unix:/run/php/php8.3-fpm.sock;
        include fastcgi_params;
        fastcgi_param SCRIPT_FILENAME /var/www/html/filelister/index.php;
    }
}
```

**Apache (`.htaccess` في دليل الهدف):**
```apache
DirectoryIndex index.php FileLister.php index.html

# توجيه جميع طلبات الدليل إلى FileLister:
RewriteEngine On
RewriteCond %{REQUEST_FILENAME} !-f
RewriteRule ^(.*)$ /filelister/index.php [QSA,L]
```

---

### وضع 5: Docker

```bash
docker-compose up -d
```

الوصول في `http://localhost:8080`. تحرير `docker-compose.yml` لتركيب دليل الهدف الخاص بك.

---

### مقارنة أوضاع النشر

| وضع | ملفات مطلوبة | الأفضل ل |
|------|---------------|----------|
| **مستقل** | `Standalone.php` فقط | نشر سريع، استضافة مشتركة |
| **عادي** | `index.php` + `core.php` + `_/` | تطوير، سيطرة كاملة |
| **دليل فرعي** | مثل العادي، موضوع في دليل فرعي | فهرسة دليل فرعي محدد |
| **عالمي** | عادي + config `base_path` | مثيل واحد يفهرس أي مسار خادم |
| **Docker** | `Dockerfile` + `docker-compose.yml` | بيئات محولة |

---

## ⚙️ التكوين

جميع الإعدادات في `$config` array في `core.php` (أو `Standalone.php`).

### عام

| مفتاح | افتراضي | وصف |
|-----|---------|-------------|
| `title` | `''` | عنوان صفحة مخصص. إذا فارغ، auto-مولد من المسار. |
| `title_prefix` | `'Index of'` | بادئة لعنوان auto-مولد. |
| `title_suffix` | `' - FileLister'` | لاحقة لعنوان auto-مولد. |
| `language` | `''` | فرض locale (`en`, `vi`, `zh`, `ja`…). auto-يكتشف إذا فارغ. |
| `allowed_langs` | (18 لغة) | لغات متاحة في قائمة اختيار dropdown. |
| `theme` | `'ocean'` | سمة افتراضية. خيارات: `light`, `dark`, `auto`, `ocean`, `forest`, `dracula`, `nord`, `high-contrast`, `cute`. |
| `view_mode` | `'list'` | عرض افتراضي. خيارات: `grid`, `list`. |
| `timezone` | `'Asia/Ho_Chi_Minh'` | سلسلة منطقة زمنية PHP. |
| `date_format` | `'Y-m-d H:i:s'` | سلسلة تنسيق تاريخ PHP. |
| `base_path` | `''` | دليل جذر لنشر عالمي/دليل فرعي. |
| `favicon_path` | `''` | مسار إلى favicon مخصص. |

### خيارات العرض

| مفتاح | افتراضي | وصف |
|-----|---------|-------------|
| `show_hidden` | `false` | عرض الملفات المخفية (تبدأ بـ `.`). |
| `show_size` | `true` | عرض عمود حجم الملف. |
| `show_date` | `true` | عرض عمود تاريخ التعديل الأخير. |
| `show_type` | `true` | عرض عمود نوع الملف (عرض قائمة). |
| `show_folder_size` | `true` | حساب أحجام الدليل (تكراري — قد يكون بطيئاً للدلائل الكبيرة). |
| `show_breadcrumb` | `true` | عرض breadcrumb تنقل. |
| `show_footer` | `true` | عرض شريط footer. |
| `show_copyright` | `true` | عرض معلومات حقوق النشر في footer. |
| `show_language_selector` | `true` | عرض تحكم محدد اللغة. |
| `show_theme_selector` | `true` | عرض زر محدد السمة. |

### الميزات

| مفتاح | افتراضي | وصف |
|-----|---------|-------------|
| `enable_search` | `true` | تمكين بحث ملف حي. |
| `enable_preview` | `true` | تمكين نموذج معاينة ملف (صور, فيديو, صوت, PDF, كود). |
| `enable_download` | `true` | عرض أزرار تنزيل على الملفات. |
| `enable_share` | `true` | تمكين نموذج مشاركة ملف مع رمز QR. |
| `enable_qrcode` | `true` | إنشاء رموز QR في نموذج المشاركة. |
| `enable_shortcuts` | `true` | تمكين اختصارات لوحة المفاتيح. |
| `enable_export` | `true` | تمكين تصدير/نسخ قائمة ملفات (JSON, CSV, TSV, NDJSON). |
| `enable_readme` | `true` | عرض ملفات `README.md` في أسفل قوائم الدليل. |
| `enable_hashtype` | `'CRC32,XXH128,SHA-256,SHA3-256'` | قائمة مفصولة بفواصل من خوارزميات hash. مدعومة: `MD5`, `SHA-1`, `SHA-256`, `SHA-512`, `SHA-512/224`, `SHA-512/256`, `SHA3-256`, `WHIRLPOOL`, `CRC32`, `XXH128`, و 30+ أخرى. |
| `hash_uppercase` | `true` | عرض قيم hash بأحرف كبيرة. |
| `max_hash_size` | `1000` | حجم ملف أقصى مسموح للـ hashing (MB). |

### الأمان

| مفتاح | افتراضي | وصف |
|-----|---------|-------------|
| `ignore_files` | (انظر أدناه) | ملفات لإخفائها. افتراضي يشمل `index.php`, `.htaccess`, `.htpasswd`, `.git`, `.env`. |
| `ignore_extensions` | `['php']` | امتدادات لإخفائها. |
| `ignore_folders` | `['_', '.git', '.svn', 'node_modules', 'vendor']` | دلائل لإخفائها. |
| `allowed_extensions` | `[]` | قائمة بيضاء للامتدادات (فارغ = السماح بكل شيء). |
| `protected_paths` | `['/etc', '/var/www/.git']` | مسارات مطلقة محظورة دائماً. |
| `enable_rate_limit` | `true` | تمكين تحديد معدل IP-based. |
| `rate_limit_requests` | `60` | طلبات قصوى لكل نافذة. |
| `rate_limit_period` | `60` | نافذة وقت تحديد معدل (ثواني). |
| `rate_limit_exclude_ips` | `['127.0.0.1', '::1']` | IPs مستثنى من تحديد معدل. |
| `trusted_proxies` | `[]` | IPs مسموح لها بتعيين `X-Forwarded-For`. فارغ = لا تثق بأحد. |
| `enable_dev` | `true` | **⚠️ حدد إلى `false` في الإنتاج.** تمكين عرض أخطاء PHP وتعطيل cache. |

> [!CAUTION]
> حدد دائماً `'enable_dev' => false` قبل النشر في الإنتاج. في وضع dev، يتم عرض أخطاء PHP مما قد يكشف مسارات ملفات، تفاصيل تكوين، و stack traces للزوار.

### متقدم

| مفتاح | افتراضي | وصف |
|-----|---------|-------------|
| `assets_path` | `''` | مسار إلى مجلد أصول `_/`. auto-مكتشف إذا فارغ. |
| `use_embedded` | `false` | فرض وضع أصول مدمجة (مستخدم من `Standalone.php`). |
| `thumbnail_directory` | `''` | مسار مخصص لـ cache thumbnails. auto-محدد إلى `_/thumbs` إذا فارغ. |
| `thumbnail_width` | `200` | عرض thumbnail أقصى (px). |
| `thumbnail_height` | `200` | ارتفاع thumbnail أقصى (px). |
| `thumbnail_cache_expiry` | `30` | أيام قبل حذف thumbnails cached. `0` = مسح دائماً. `-1` = لا تمسح أبداً. |
| `readme_files` | (قائمة) | أسماء ملفات لفحص عرض README. |
| `custom_css` | `'_/css/custom.css'` | مسار إلى ملف CSS مخصص (محمل إذا موجود). |
| `custom_js` | `'_/js/custom.js'` | مسار إلى ملف JS مخصص (محمل إذا موجود). |
| `serve_index_files` | `false` | خدم `index.html` مباشرة إذا موجود. ⚠️ خطر XSS محتمل إذا كانت ملفات غير موثوقة موجودة. |
| `index_files` | `['index.html', …]` | أسماء ملفات فهرس للبحث عنها. |

### تعيين الخادم كفهرس دليل

#### Nginx
```nginx
index index.php FileLister.php index.html;
```

#### Apache (`.htaccess`)
```apache
DirectoryIndex index.php FileLister.php index.html
```

### السماح بمضيفين خارجيين (CSP)
FileLister يستخدم **Content Security Policy** صارم. لتحميل موارد من نطاقات خارجية، عدل رأس `Content-Security-Policy` في `core.php`:

```php
// أضف نطاقك إلى التوجيه المناسب:
// img-src: للصور الخارجية
// script-src: للسكريبتات الخارجية (استخدم بحذر)
// style-src: لـ CSS خارجي
```

---

## 🎨 تخصيص السمة

### السمات المتاحة
| سمة | وصف |
|-------|-------------|
| `light` | سمة بيضاء نظيفة |
| `dark` | وضع مظلم |
| `auto` | اتبع تفضيل النظام |
| `ocean` | ألوان محيط زرقاء |
| `forest` | ألوان أرض خضراء |
| `dracula` | بنفسجي مظلم Dracula |
| `nord` | لوحة قطبية شمالية |
| `high-contrast` | تركيز إمكانية الوصول |
| `cute` | glassmorphism أنمي مع صورة خلفية |

### إنشاء سمة مخصصة

1. **نسخ سمة**: استنسخ `_/css/themes/ocean.css` → `_/css/themes/mytheme.css`.

2. **تحرير متغيرات CSS**:
```css
[data-theme="mytheme"] {
    --bg-primary: #1a1a2e;
    --accent-primary: #e94560;
    /* ... متغيرات أخرى */
}
```

3. **تسجيل في JS**: أضف اسم سمةك إلى `toggleTheme()` array في `_/js/app.js`.

4. **تفعيل في config**:
```php
'theme' => 'mytheme',
```

5. **قائمة بيضاء في config** (ليعمل محدد السمة): في `index.php`, ابحث عن `$allowed_themes` وأضف `'mytheme'` إلى array.

---

## 🧩 خطافات HTML مخصصة

حقن HTML, CSS أو JavaScript مخصص في مواقع صفحة محددة دون تحرير ملفات core. قم بتكوين `html_hooks` array في `core.php`:

```php
'html_hooks' => array(
    'head_end'      => '',  // قبل </head>
    'body_start'    => '',  // بعد <body>
    'header_start'  => '',  // بعد <header> يفتح
    'header_end'    => '',  // قبل </header>
    'main_before'   => '',  // قبل <main>
    'main_start'    => '',  // داخل <main>, قبل العناصر
    'main_end'      => '',  // داخل <main>, بعد العناصر
    'main_after'    => '',  // بعد </main>
    'footer_before' => '',  // قبل <footer>
    'footer_start'  => '',  // بعد <footer> يفتح
    'footer_end'    => '',  // قبل </footer>
    'footer_after'  => '',  // بعد </footer>
    'body_end'      => '',  // قبل </body>
    'html_end'      => '',  // قبل </html>
),
```

### مثال: Google Analytics
```php
'head_end' => '<script async src="https://www.googletagmanager.com/gtag/js?id=G-XXXXXX"></script>
<script>window.dataLayer=window.dataLayer||[];function gtag(){dataLayer.push(arguments);}gtag("js",new Date());gtag("config","G-XXXXXX");</script>',
```

---

## 🌍 دعم متعدد اللغات
FileLister يكتشف تلقائياً لغة المتصفح ويدعم **18+ لغة**:

`en` `vi` `zh` `ja` `ko` `es` `fr` `de` `it` `nl` `sv` `no` `da` `fi` `he` `ar` `ru`

حدد لغة ثابتة مع `'language' => 'vi'`, أو اترك فارغاً للـ auto-اكتشاف.

---

## 👁️ معاينة الملف & Viewer
عارض أداء عالي مدمج لأنواع ملفات متنوعة:
- **صور**: jpg, png, gif, webp, svg (thumbnails في الوقت الفعلي في عرض الشبكة)
- **فيديو**: mp4, webm, avi, mov, mkv
- **صوت**: mp3, ogg, flac, wav, m4a
- **وثائق**: عارض PDF مدمج وعرض Markdown
- **كود**: syntax highlighting عبر Prism.js لـ 100+ لغة

---

## 🔗 مشاركة & تنزيل
- إنشاء **رموز QR فورية** لنقل ملفات محمولة.
- روابط تنزيل مباشرة لجميع الملفات.
- مشاركة ملفات آمنة عبر URLs فريدة.
- **دعم Unicode كامل**: أسماء ملفات بالعربية, الصينية, اليابانية, العبرية, وscripts غير ASCII أخرى مشفرة بشكل صحيح بالنسبة المئوية في روابط المشاركة ورموز QR.

---

## ⌨️ اختصارات لوحة المفاتيح
| مفتاح | إجراء |
|-----|--------|
| `/` أو `Ctrl+F` | التركيز على مربع البحث |
| `Esc` | إغلاق النموذج / مسح البحث |
| `↑` / `↓` | التنقل عبر العناصر |
| `Enter` | فتح العنصر المحدد |
| `g` ثم `h` | الذهاب إلى المنزل (الجذر) |
| `g` ثم `u` | الذهاب لأعلى مستوى دليل واحد |
| `?` | عرض مساعدة اختصارات لوحة المفاتيح |

---

## 🛡️ تفاصيل الأمان

FileLister يشمل طبقات أمان محسنة متعددة:

| طبقة | تفصيل |
|-------|--------|
| **مسار الطريق** | إدخال `?dir=` مثبت مع `realpath()` ومرتبط بـ `$lister_root`. |
| **CSP Nonce** | nonce عشوائي 128-bit لكل طلب على جميع السكريبتات الداخلية. لا `unsafe-inline`. |
| **تحديد المعدل** | throttling طلبات IP-based محفوظة في ملفات مؤقتة. افتراضي: 60 req/60s. |
| **وكلاء موثوقين** | `X-Forwarded-For` موثوق فقط من IPs وكيل محددة صراحة. |
| **ملفات حساسة** | `.env`, `.git`, `.htaccess`, ملفات PHP مخفية تلقائياً. |
| **رؤوس الأمان** | `X-Frame-Options: SAMEORIGIN`, `X-Content-Type-Options: nosniff`, `Permissions-Policy` لإزالة كاميرا/mic/geo. |
| **HSTS** | `Strict-Transport-Security` مرسل تلقائياً عند HTTPS. |
| **CORS** | endpoint تصدير يسمح فقط بطلبات same-origin. لا انعكاس origin تعسفي. |
| **لا hashes قديمة** | MD5 و SHA-1 مستثنى من أنواع hash افتراضية. |
| **حماية symlink** | symlinks متخطى أثناء traversal دليل لمنع حلقات وتسربات. |
| **وضع Dev** | `enable_dev: false` في الإنتاج يعطل عرض الأخطاء. |

> [!IMPORTANT]
> بعد التكوين, حدد فوراً `'enable_dev' => false` لمنع رسائل الخطأ من كشف internals الخادم.

---

## 📋 المتطلبات
- **PHP**: 5.2 أو أعلى (مختبر حتى PHP 8.4+)
- **امتدادات**: `json` (مطلوب), `gd` (اختياري — للـ thumbnails), `zip` (اختياري)

---

## 📜 سجل التغييرات

### v1.5.36 — إصدار أمان & إصلاح أخطاء

**إصلاحات أمان:**
- 🔒 **[حرج] إصلاح ثغرة انعكاس CORS** في endpoint `?export=` — لا يعكس headers `Origin` تعسفية بعد الآن
- 🔒 **[حرج] إصلاح XSS في معاينة ملف** — اسم ملف في معاينة "نوع غير مدعوم" لم يكن escaped قبل حقن DOM
- 🔒 **[حرج] `enable_dev` الآن افتراضي `false`** — يمنع كشف خطأ PHP غير مقصود في الإنتاج
- 🔒 **[عالي] تم التحقق من `dir_theme` cookie** قبل الاستخدام لمنع سلوك غير متوقع

**إصلاحات أخطاء:**
- 🐛 **إصلاح فشل إنشاء QR code** لملفات بأسماء Unicode (فيتنامية, صينية, يابانية, إلخ)
- 🐛 **إصلاح رابط مشاركة مكسور** لملفات بأسماء ملفات Unicode/غير ASCII
- 🐛 **إصلاح عدم تحميل معاينة صورة** لملفات بأسماء ملفات Unicode
- 🐛 **إصلاح علامة `</div>` مكررة** في HTML footer (تسببت في مشاكل تخطيط في بعض المتصفحات)
- 🐛 **إصلاح `style.css` محمل مرتين** (إهدار عرض النطاق, تحليل مزدوج)
- 🐛 **إصلاح ملفات مفقودة `custom.js` / `custom.css`** في build `Standalone.php`
- 🐛 **إصلاح استعادة سمة** — سمات `dracula`, `nord`, `high-contrast`, `cute` لا تُعاد تعيينها عند إعادة تحميل الصفحة
- 🐛 **إصلاح أيقونات SVG مكررة** محقونة مع thumbnails في عرض الشبكة
- 🐛 **إصلاح parsing تكوين تنقل AJAX** — regex أكثر قوة بدلاً من استخراج مبني على فهرس هش
- 🐛 **إصلاح `previewText()` يعرض HTML 404** كمحتوى ملف عندما يكون الملف غير قابل للوصول
- 🐛 **إصلاح كود ميت `changeLanguage()`** يشير إلى عنصر `langToggle` غير موجود
- 🐛 **أضيف SHA-512/224 و SHA-512/256** إلى خريطة خوارزمية hash (مذكورة في docs ولكن مفقودة في الكود)
- 🐛 **استبدلت استدعاءات `alert()`** في نسخ clipboard مع إشعارات toast غير حاجزة
- 🐛 **إصلاح تنقل معرض صور** — صور مخفية بواسطة مرشح/بحث مستثنى الآن من traversal prev/next
- 🐛 **إصلاح معاينات `audio`/`video`** — معالج خطأ مضاف عندما يفشل الوسائط في التحميل

---

## ☕ دعم عملي
استمتعت بهذا السكريبت المفتوح المصدر PHP؟
- [اشتر لي 🍻](https://buymeacoffee.com/trong)
- تبرع عبر ❤️ [PayPal](https://paypal.me/DaoVanTrong)

---

## 📝 الترخيص
ترخيص MIT — © 2026 [TRONG.PRO](https://trong.pro)

<!--
  ___ _ _     _    _    _           
 | __(_) |___| |  (_).__| |_ ___ _ _ 
 | _|| | / -_) |__| (_-<  _/ -_) '_|
 |_| |_|_\___|____|_/__/\__\___|_|  
 End FileLister README
-->
