<!--
  ___ _ _     _    _    _           
 | __(_) |___| |  (_)__| |_ ___ _ _ 
 | _|| | / -_) |__| (_-<  _/ -_) '_|
 |_| |_|_\___|____|_/__/\__\___|_|  
 FileLister by TRONG.PRO
-->

# 📂 FileLister: Современный скрипт листинга каталогов PHP v1.5.36

FileLister - это мощный, легковесный и современный **скрипт листинга каталогов PHP**, который преобразует ваши серверные файлы в красивый, мобильно-дружественный **веб-файл эксплорер**. Это идеальная альтернатива `h5ai` или `Apache Index`, предлагающая опцию развертывания одного файла и встроенные предварительные просмотры файлов.

![PHP Version](https://img.shields.io/badge/PHP-%3E%3D5.2-blue)
![License](https://img.shields.io/badge/license-MIT-green)
![Version](https://img.shields.io/badge/version-1.5.36-orange)
[![GitHub](https://img.shields.io/badge/GitHub-Repo-181717?logo=github)](https://github.com/daovantrong/filelister)

[🇬🇧 English](README.md) | [🇻🇳 Tiếng Việt](README.vi.md) | [🇨🇳 简体中文](README.zh.md) | [🇪🇸 Español](README.es.md) | [🇫🇷 Français](README.fr.md) | [🇩🇪 Deutsch](README.de.md) | [🇯🇵 日本語](README.ja.md) | [🇰🇷 한국어](README.kr.md) | [🇮🇹 Italiano](README.it.md) | [🇳🇱 Nederlands](README.nl.md) | [🇸🇪 Svenska](README.sv.md) | [🇳🇴 Norsk](README.no.md) | [🇩🇰 Dansk](README.da.md) | [🇫🇮 Suomi](README.fi.md) | [🇮🇱 עברית](README.he.md) | [🇦🇪 العربية](README.ar.md) | [🇷🇺 Русский](README.ru.md)

---

## 📖 Оглавление
- [✨ Функции](#-функции)
- [📦 Установка](#-установка)
- [⚙️ Конфигурация](#-конфигурация)
- [🎨 Темы](#-темы)
- [🧩 Пользовательские HTML хуки](#-пользовательские-html-хуки)
- [🌍 Многоязычная поддержка](#-многоязычная-поддержка)
- [👁️ Предварительный просмотр файла](#-предварительный-просмотр-файла--viewer)
- [🔗 Поделиться & Скачать](#-поделиться--скачать)
- [⌨️ Горячие клавиши](#-горячие-клавиши)
- [🛡️ Детали безопасности](#-детали-безопасности)
- [📋 Требования](#-требования)

---

## ✨ Функции

### 🚀 **Готов к производству & Быстрый**
- **Автономная версия**: Развертывание одного файла (`Standalone.php`) со всеми встроенными активами. Запустите `php build.php` для генерации.
- **Поддержка Docker**: Готовые `Dockerfile` и `docker-compose.yml`.
- **Обслуживать индекс**: Опционально обслуживать `index.html`, если присутствует в каталоге.

### 🎨 **Современный пользовательский интерфейс**
- **Чистый & Адаптивный**: Mobile-first макет, работает на любом устройстве.
- **9 тем**: `light`, `dark`, `auto`, `ocean`, `forest`, `dracula`, `nord`, `high-contrast`, `cute` (аниме glassmorphism).
- **Виды сетки & списка**: Переключение между видами сетки карт и детального списка.
- **Рендеринг README**: Автоматический рендеринг файлов `README.md` в нижней части списков каталогов.
- **Локализованный**: Выбор языка с 18+ поддерживаемыми локалями.

### 🛡️ **Безопасность усилена**
- **CSP с nonces**: Запрос-per-криптографический nonce на всех встроенных скриптах. Без `unsafe-inline`.
- **Ограничение скорости**: Интегрированное throttling запросов анти-DDoS (60 req/60s по умолчанию).
- **Доверенные прокси**: Безопасная обработка `X-Forwarded-For` — применяется только если запрос приходит от доверенного IP прокси.
- **Защита обхода пути**: Все входные данные `?dir=` разрешаются через `realpath()` и связываются с `$lister_root`.
- **Скрытие чувствительных файлов**: Автоматическое игнорирование `.env`, `.git`, `.htaccess`, и файлов PHP.
- **Заголовки безопасности**: `X-Frame-Options`, `X-Content-Type-Options`, `X-XSS-Protection`, `Referrer-Policy`, `Permissions-Policy`, `Strict-Transport-Security` (только HTTPS).
- **Без MD5/SHA-1**: Набор hash по умолчанию установлен на `CRC32,XXH128,SHA-256,SHA3-256`. MD5 и SHA-1 исключены по умолчанию.

### 🔍 **Целостность файла (Инфо & Hash)**
- Проверка 40+ алгоритмов hash на файл, включая SHA-3, WHIRLPOOL, XXH128, CRC32.
- Настраиваемый максимальный размер файла для hashing.
- Результаты отображаются inline в модальном окне Info.

### 📤 **Экспорт & Поделиться**
- Копировать/Скачивать список файлов в **JSON, CSV, TSV, NDJSON** форматах.
- Делиться файлами через QR коды и прямые ссылки.

---

## 📦 Установка & Режимы развертывания

FileLister поддерживает 4 режима развертывания. Выберите тот, который подходит для вашей конфигурации:

---

### Режим 1: Автономный (Один файл PHP) — Рекомендуется для производства

Все активы скомпилированы в самодостаточный файл. Папка `_/` не нужна.

```bash
# Шаг 1: Построить автономный файл
php build.php

# Шаг 2: Загрузить Standalone.php на ваш сервер
# Шаг 3: Переименовать в index.php (или любое имя, которое вы хотите)
```

> **Config**: Автоматически устанавливает `'use_embedded' => true`. Другой config не нужен.

---

### Режим 2: Нормальный (Исходные файлы)

Классическая конфигурация много-файлов. Самый быстрый для разработки.

```
your-web-root/
├── index.php        ← Точка входа (require_once 'core.php')
├── core.php         ← Логика ядра & config
└── _/               ← Файлы CSS, JS, иконки, переводы
```

**Шаги:**
1. Скопировать `index.php`, `core.php`, и `_/` в ваш веб-каталог.
2. Доступ через браузер: `http://yoursite.com/`
3. Дополнительный config не нужен.

---

### Режим 3: Развертывание подкаталога

Запуск FileLister в подкаталоге, который индексирует свое собственное содержимое.

```
your-web-root/
├── files/           ← Каталог, который вы хотите индексировать
│   ├── index.php    ← Точка входа FileLister
│   └── core.php
└── _/               ← Общие активы (auto-обнаруженные через сканирование родителя)
```

Функция `detect_assets_path()` автоматически сканирует **до 5 родительских каталогов** для определения папки активов `_/`. Ручной config `assets_path` не требуется в большинстве случаев.

Если активы не auto-обнаружены:
```php
'assets_path' => '../_',   // Или полный путь как '/var/www/html/_'
```

---

### Режим 4: Глобальное развертывание (Индексировать любой каталог на сервере)

Использовать **одну установку FileLister** для просмотра любого пути на сервере, отделенного от местоположения скрипта.

```
/var/www/html/
├── filelister/      ← FileLister живет здесь
│   ├── index.php
│   ├── core.php
│   └── _/
└── data/            ← Каталог, который вы действительно хотите индексировать
```

**Конфигурация в `core.php`:**
```php
'base_path' => '/var/data',   // ← Установить каталог, который вы хотите перечислить
```

> `base_path` принимает любой **абсолютный путь файловой системы**, который процесс PHP может читать. Скрипт будет вынуждать всю навигацию `?dir=` оставаться в этом корне через `realpath()` для предотвращения обхода пути.

**Конфигурация веб-сервера** (для использования FileLister как индекса каталога):

**Nginx:**
```nginx
server {
    root /var/data;
    index index.php FileLister.php index.html;

    location / {
        try_files $uri $uri/ /index.php?$query_string;
    }
    location ~ \.php$ {
        fastcgi_pass unix:/run/php/php8.3-fpm.sock;
        include fastcgi_params;
        fastcgi_param SCRIPT_FILENAME /var/www/html/filelister/index.php;
    }
}
```

**Apache (`.htaccess` в целевом каталоге):**
```apache
DirectoryIndex index.php FileLister.php index.html

# Маршрутизировать все запросы каталога к FileLister:
RewriteEngine On
RewriteCond %{REQUEST_FILENAME} !-f
RewriteRule ^(.*)$ /filelister/index.php [QSA,L]
```

---

### Режим 5: Docker

```bash
docker-compose up -d
```

Доступ на `http://localhost:8080`. Редактировать `docker-compose.yml` для монтирования вашего целевого каталога.

---

### Сравнение режимов развертывания

| Режим | Необходимые файлы | Лучше для |
|------|---------------|----------|
| **Автономный** | Только `Standalone.php` | Быстрое развертывание, общий хостинг |
| **Нормальный** | `index.php` + `core.php` + `_/` | Разработка, полный контроль |
| **Подкаталог** | То же, что и Нормальный, размещенный в подкаталоге | Индексация конкретного подкаталога |
| **Глобальный** | Нормальный + config `base_path` | Один экземпляр индексации любого пути сервера |
| **Docker** | `Dockerfile` + `docker-compose.yml` | Контейнеризованные среды |

---

## ⚙️ Конфигурация

Все настройки в массиве `$config` в `core.php` (или `Standalone.php`).

### Общее

| Ключ | По умолчанию | Описание |
|-----|---------|-------------|
| `title` | `''` | Пользовательский заголовок страницы. Если пусто, auto-сгенерировано от пути. |
| `title_prefix` | `'Index of'` | Префикс для auto-сгенерированного заголовка. |
| `title_suffix` | `' - FileLister'` | Суффикс для auto-сгенерированного заголовка. |
| `language` | `''` | Принудить локаль (`en`, `vi`, `zh`, `ja`…). Auto-обнаруживает если пусто. |
| `allowed_langs` | (18 языков) | Языки доступные в выпадающем списке селектора. |
| `theme` | `'ocean'` | Тема по умолчанию. Опции: `light`, `dark`, `auto`, `ocean`, `forest`, `dracula`, `nord`, `high-contrast`, `cute`. |
| `view_mode` | `'list'` | Вид по умолчанию. Опции: `grid`, `list`. |
| `timezone` | `'Asia/Ho_Chi_Minh'` | Строка часового пояса PHP. |
| `date_format` | `'Y-m-d H:i:s'` | Строка формата даты PHP. |
| `base_path` | `''` | Корневой каталог для глобального/подкаталог развертывания. |
| `favicon_path` | `''` | Путь к пользовательскому favicon. |

### Опции отображения

| Ключ | По умолчанию | Описание |
|-----|---------|-------------|
| `show_hidden` | `false` | Показывать скрытые файлы (начинающиеся с `.`). |
| `show_size` | `true` | Показывать столбец размера файла. |
| `show_date` | `true` | Показывать столбец даты последнего изменения. |
| `show_type` | `true` | Показывать столбец типа файла (вид списка). |
| `show_folder_size` | `true` | Рассчитывать размеры папок (рекурсивно — может быть медленным для больших папок). |
| `show_breadcrumb` | `true` | Показывать breadcrumb навигации. |
| `show_footer` | `true` | Показывать панель footer. |
| `show_copyright` | `true` | Показывать информацию об авторских правах в footer. |
| `show_language_selector` | `true` | Показывать элемент управления селектора языка. |
| `show_theme_selector` | `true` | Показывать кнопку селектора темы. |

### Функции

| Ключ | По умолчанию | Описание |
|-----|---------|-------------|
| `enable_search` | `true` | Включить живой поиск файлов. |
| `enable_preview` | `true` | Включить модальное окно предварительного просмотра файлов (изображения, видео, аудио, PDF, код). |
| `enable_download` | `true` | Показывать кнопки скачивания на файлах. |
| `enable_share` | `true` | Включить модальное окно поделиться файлами с QR кодом. |
| `enable_qrcode` | `true` | Генерировать QR коды в модальном окне поделиться. |
| `enable_shortcuts` | `true` | Включить горячие клавиши. |
| `enable_export` | `true` | Включить экспорт/копирование списка файлов (JSON, CSV, TSV, NDJSON). |
| `enable_readme` | `true` | Рендерить файлы `README.md` в нижней части списков каталогов. |
| `enable_hashtype` | `'CRC32,XXH128,SHA-256,SHA3-256'` | Список разделенных запятыми алгоритмов hash. Поддерживаемые: `MD5`, `SHA-1`, `SHA-256`, `SHA-512`, `SHA-512/224`, `SHA-512/256`, `SHA3-256`, `WHIRLPOOL`, `CRC32`, `XXH128`, и 30+ другие. |
| `hash_uppercase` | `true` | Отображать значения hash в верхнем регистре. |
| `max_hash_size` | `1000` | Максимальный размер файла разрешенный для hashing (MB). |

### Безопасность

| Ключ | По умолчанию | Описание |
|-----|---------|-------------|
| `ignore_files` | (см. ниже) | Файлы для скрытия. По умолчанию включает `index.php`, `.htaccess`, `.htpasswd`, `.git`, `.env`. |
| `ignore_extensions` | `['php']` | Расширения для скрытия. |
| `ignore_folders` | `['_', '.git', '.svn', 'node_modules', 'vendor']` | Папки для скрытия. |
| `allowed_extensions` | `[]` | Белый список расширений (пусто = разрешить все). |
| `protected_paths` | `['/etc', '/var/www/.git']` | Абсолютные пути всегда блокируемые. |
| `enable_rate_limit` | `true` | Включить ограничение скорости на основе IP. |
| `rate_limit_requests` | `60` | Максимальные запросы на окно. |
| `rate_limit_period` | `60` | Окно времени ограничения скорости (секунды). |
| `rate_limit_exclude_ips` | `['127.0.0.1', '::1']` | IPs исключенные из ограничения скорости. |
| `trusted_proxies` | `[]` | IPs разрешенные устанавливать `X-Forwarded-For`. Пусто = не доверять никому. |
| `enable_dev` | `true` | **⚠️ Установить в `false` в производстве.** Включает отображение ошибок PHP и отключает cache. |

> [!CAUTION]
> Всегда устанавливайте `'enable_dev' => false` перед развертыванием в производство. В режиме dev отображаются ошибки PHP, что может раскрыть пути файлов, детали конфигурации, и stack traces посетителям.

### Продвинутый

| Ключ | По умолчанию | Описание |
|-----|---------|-------------|
| `assets_path` | `''` | Путь к папке активов `_/`. Auto-обнаружено если пусто. |
| `use_embedded` | `false` | Принудить режим встроенных активов (используемый `Standalone.php`). |
| `thumbnail_directory` | `''` | Пользовательский путь для кэша thumbnails. Auto-установлено в `_/thumbs` если пусто. |
| `thumbnail_width` | `200` | Максимальная ширина thumbnail (px). |
| `thumbnail_height` | `200` | Максимальная высота thumbnail (px). |
| `thumbnail_cache_expiry` | `30` | Дни перед очисткой кэшированных thumbnails. `0` = всегда очищать. `-1` = никогда не очищать. |
| `readme_files` | (список) | Имена файлов для сканирования для рендеринга README. |
| `custom_css` | `'_/css/custom.css'` | Путь к пользовательскому файлу CSS (загружается если существует). |
| `custom_js` | `'_/js/custom.js'` | Путь к пользовательскому файлу JS (загружается если существует). |
| `serve_index_files` | `false` | Обслуживать `index.html` напрямую если присутствует. ⚠️ Потенциальный риск XSS если присутствуют не доверенные файлы. |
| `index_files` | `['index.html', …]` | Имена файлов индекса для поиска. |

### Установить сервер как индекс каталога

#### Nginx
```nginx
index index.php FileLister.php index.html;
```

#### Apache (`.htaccess`)
```apache
DirectoryIndex index.php FileLister.php index.html
```

### Разрешить внешние хосты (CSP)
FileLister использует строгую **Content Security Policy**. Для загрузки ресурсов из внешних доменов, отредактируйте заголовок `Content-Security-Policy` в `core.php`:

```php
// Добавьте ваш домен к соответствующей директиве:
// img-src: для внешних изображений
// script-src: для внешних скриптов (используйте с осторожностью)
// style-src: для внешнего CSS
```

---

## 🎨 Кастомизация темы

### Доступные темы
| Тема | Описание |
|-------|-------------|
| `light` | Чистая белая тема |
| `dark` | Темный режим |
| `auto` | Следовать предпочтению системы |
| `ocean` | Синие океанические тона |
| `forest` | Зеленые земные тона |
| `dracula` | Темно-фиолетовый Dracula |
| `nord` | Северная арктическая палитра |
| `high-contrast` | Фокус на доступности |
| `cute` | Glassmorphism аниме с фоновым изображением |

### Создать пользовательскую тему

1. **Скопировать тему**: Дублировать `_/css/themes/ocean.css` → `_/css/themes/mytheme.css`.

2. **Редактировать CSS переменные**:
```css
[data-theme="mytheme"] {
    --bg-primary: #1a1a2e;
    --accent-primary: #e94560;
    /* ... другие переменные */
}
```

3. **Зарегистрировать в JS**: Добавить имя вашей темы в массив `toggleTheme()` в `_/js/app.js`.

4. **Активировать в config**:
```php
'theme' => 'mytheme',
```

5. **Whitelist в config** (чтобы работал селектор темы): В `index.php`, поискать `$allowed_themes` и добавить `'mytheme'` в массив.

---

## 🧩 Пользовательские HTML хуки

Инжектировать пользовательский HTML, CSS или JavaScript в специфические позиции страницы без редактирования файлов core. Конфигурировать массив `html_hooks` в `core.php`:

```php
'html_hooks' => array(
    'head_end'      => '',  // Перед </head>
    'body_start'    => '',  // После <body>
    'header_start'  => '',  // После <header> открывается
    'header_end'    => '',  // Перед </header>
    'main_before'   => '',  // Перед <main>
    'main_start'    => '',  // Внутри <main>, перед элементами
    'main_end'      => '',  // Внутри <main>, после элементов
    'main_after'    => '',  // После </main>
    'footer_before' => '',  // Перед <footer>
    'footer_start'  => '',  // После <footer> открывается
    'footer_end'    => '',  // Перед </footer>
    'footer_after'  => '',  // После </footer>
    'body_end'      => '',  // Перед </body>
    'html_end'      => '',  // Перед </html>
),
```

### Пример: Google Analytics
```php
'head_end' => '<script async src="https://www.googletagmanager.com/gtag/js?id=G-XXXXXX"></script>
<script>window.dataLayer=window.dataLayer||[];function gtag(){dataLayer.push(arguments);}gtag("js",new Date());gtag("config","G-XXXXXX");</script>',
```

---

## 🌍 Многоязычная поддержка
FileLister auto-обнаруживает язык браузера и поддерживает **18+ языков**:

`en` `vi` `zh` `ja` `ko` `es` `fr` `de` `it` `nl` `sv` `no` `da` `fi` `he` `ar` `ru`

Установить фиксированный язык с `'language' => 'vi'`, или оставить пустым для auto-обнаружения.

---

## 👁️ Предварительный просмотр файла & Viewer
Интегрированный высокопроизводительный viewer для различных типов файлов:
- **Изображения**: jpg, png, gif, webp, svg (реальные thumbnails в виде сетки)
- **Видео**: mp4, webm, avi, mov, mkv
- **Аудио**: mp3, ogg, flac, wav, m4a
- **Документы**: Интегрированный PDF viewer и рендеринг Markdown
- **Код**: Syntax highlighting через Prism.js для 100+ языков

---

## 🔗 Поделиться & Скачать
- Генерировать мгновенные **QR коды** для мобильных передач файлов.
- Прямые ссылки скачивания для всех файлов.
- Безопасное деление файлов через уникальные URLs.
- **Полная поддержка Unicode**: имена файлов на вьетнамском, китайском, японском, арабском, и других не-ASCII скриптах правильно percent-encoded в ссылках поделиться и QR кодах.

---

## ⌨️ Горячие клавиши
| Клавиша | Действие |
|-----|--------|
| `/` или `Ctrl+F` | Фокус на поле поиска |
| `Esc` | Закрыть модальное окно / очистить поиск |
| `↑` / `↓` | Навигация по элементам |
| `Enter` | Открыть выбранный элемент |
| `g` затем `h` | Перейти домой (корень) |
| `g` затем `u` | Перейти на один уровень каталога выше |
| `?` | Показать помощь горячих клавиш |

---

## 🛡️ Детали безопасности

FileLister включает несколько усиленных слоев безопасности:

| Слой | Деталь |
|-------|--------|
| **Обход пути** | Вход `?dir=` валидирован с `realpath()` и связан с `$lister_root`. |
| **CSP Nonce** | Случайный 128-bit nonce на запрос для всех встроенных скриптов. Без `unsafe-inline`. |
| **Ограничение скорости** | Throttling запросов на основе IP сохранено в временных файлах. По умолчанию: 60 req/60s. |
| **Доверенные прокси** | `X-Forwarded-For` доверен только от явно настроенных IP прокси. |
| **Чувствительные файлы** | `.env`, `.git`, `.htaccess`, файлы PHP автоматически скрыты. |
| **Заголовки безопасности** | `X-Frame-Options: SAMEORIGIN`, `X-Content-Type-Options: nosniff`, `Permissions-Policy` для отключения камеры/mic/geo. |
| **HSTS** | `Strict-Transport-Security` отправлено автоматически при HTTPS. |
| **CORS** | Endpoint экспорта разрешает только same-origin запросы. Без произвольного отражения origin. |
| **Без старых hashes** | MD5 и SHA-1 исключены из типов hash по умолчанию. |
| **Защита symlink** | Symlinks пропущены во время traversal каталога для предотвращения циклов и утечек. |
| **Режим Dev** | `enable_dev: false` в производстве отключает отображение ошибок. |

> [!IMPORTANT]
> После конфигурации, немедленно установите `'enable_dev' => false` для предотвращения сообщений об ошибках от раскрытия internals сервера.

---

## 📋 Требования
- **PHP**: 5.2 или выше (тестировано до PHP 8.4+)
- **Расширения**: `json` (требуется), `gd` (опционально — для thumbnails), `zip` (опционально)

---

## 📜 Журнал изменений

### v1.5.36 — Релиз безопасности & исправления ошибок

**Исправления безопасности:**
- 🔒 **[Критично] Исправлена уязвимость отражения CORS** в endpoint `?export=` — больше не отражает произвольные заголовки `Origin`
- 🔒 **[Критично] Исправлен XSS в предварительном просмотре файла** — имя файла в предварительном просмотре "неподдерживаемый тип" не было escaped перед инъекцией DOM
- 🔒 **[Критично] `enable_dev` теперь по умолчанию `false`** — предотвращает непреднамеренное раскрытие ошибок PHP в производстве
- 🔒 **[Высокий] Проверен cookie `dir_theme`** перед использованием для предотвращения неожиданного поведения

**Исправления ошибок:**
- 🐛 **Исправлено неудачное создание QR кода** для файлов с именами Unicode (вьетнамский, китайский, японский, и т.д.)
- 🐛 **Исправлена сломанная ссылка поделиться** для файлов с именами файлов Unicode/не-ASCII
- 🐛 **Исправлена не загружающаяся предварительная просмотр изображения** для файлов с именами файлов Unicode
- 🐛 **Исправлен дублированный тег `</div>`** в HTML footer (вызвал проблемы layout в некоторых браузерах)
- 🐛 **Исправлено `style.css` загружено дважды** (трата bandwidth, двойной parse)
- 🐛 **Исправлены отсутствующие файлы `custom.js` / `custom.css`** в build `Standalone.php`
- 🐛 **Исправлено восстановление темы** — темы `dracula`, `nord`, `high-contrast`, `cute` не сбрасываются при перезагрузке страницы
- 🐛 **Исправлены дублированные иконки SVG** инжектированные вместе с thumbnails в виде сетки
- 🐛 **Исправлено parsing конфигурации навигации AJAX** — более robust regex вместо хрупкого извлечения на основе индекса
- 🐛 **Исправлено `previewText()` отображает HTML 404** как содержимое файла когда файл недоступен
- 🐛 **Исправлен мертвый код `changeLanguage()`** ссылающийся на несуществующий элемент `langToggle`
- 🐛 **Добавлено SHA-512/224 и SHA-512/256** в карту алгоритмов hash (упомянуто в docs но отсутствовало в коде)
- 🐛 **Заменены вызовы `alert()`** в копировании clipboard на не блокирующие уведомления toast
- 🐛 **Исправлена навигация галереи изображений** — изображения скрытые фильтром/поиском теперь исключены из traversal prev/next
- 🐛 **Исправлены предварительные просмотры `audio`/`video`** — добавлен обработчик ошибок когда медиа не загружается

---

## ☕ Поддержите мою работу
Наслаждаетесь этим open-source PHP скриптом?
- [Купите мне 🍻](https://buymeacoffee.com/trong)
- Пожертвуйте через ❤️ [PayPal](https://paypal.me/DaoVanTrong)

---

## 📝 Лицензия
Лицензия MIT — © 2026 [TRONG.PRO](https://trong.pro)

<!--
  ___ _ _     _    _    _           
 | __(_) |___| |  (_).__| |_ ___ _ _ 
 | _|| | / -_) |__| (_-<  _/ -_) '_|
 |_| |_|_\___|____|_/__/\__\___|_|  
 End FileLister README
-->
